<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Comments">
	<typeAlias alias="str" type="java.lang.String" />
	<typeAlias alias="int" type="java.lang.Integer" />
	<typeAlias alias="comment" type="com.comments.db.Comments" />
	
	<select id="getCommentsList" resultClass="comment">
		select * from comments order by num desc
	</select>
	
	<insert id="insertComment" parameterClass="comment">
		INSERT INTO COMMENTS
			(num, boNum, userNum, content, regdate)
		VALUES
			(com_seq.nextval, #boNum#, #userNum#, #content#, sysdate)
	</insert>

</sqlMap>